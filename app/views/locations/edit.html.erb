<%# Edit location on a festival: /festivals/x/locations/edit -%>
<% content_for :title do %>Edit <%= h @location.name %> on <%= h @festival.name %><% end %>

<%= render :partial => "festivals/edit_header",
           :locals => { :festival => @festival } %>

<h3><%= yield :title %></h3>

<%= render :partial => "locations/form",
           :object => @location,
           :locals => { :festival => @festival } %>
